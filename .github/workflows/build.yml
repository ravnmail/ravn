name: Build

on:
  push:
    branches:
      - main
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'
  pull_request:
    branches:
      - main

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  # Application environment variables
  DATABASE_URL: "sqlite://./ravn.db"
  OFFICE365_CLIENT_ID: ${{ secrets.OFFICE365_CLIENT_ID }}
  OFFICE365_CLIENT_SECRET: ${{ secrets.OFFICE365_CLIENT_SECRET }}
  OFFICE365_TENANT: ${{ secrets.OFFICE365_TENANT }}
  GMAIL_CLIENT_ID: ${{ secrets.GMAIL_CLIENT_ID }}
  GMAIL_CLIENT_SECRET: ${{ secrets.GMAIL_CLIENT_SECRET }}
  # Toolchain environment variables
  APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
  APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
  APPLE_ID: ${{ secrets.APPLE_ID }}
  APPLE_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
  APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
  KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
  # Auto-update signing
  TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
  TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

jobs:
  determine-build-type:
    name: Determine Build Type
    runs-on: ubuntu-latest
    outputs:
      build-type: ${{ steps.determine.outputs.build-type }}
      version: ${{ steps.determine.outputs.version }}
      short-hash: ${{ steps.determine.outputs.short-hash }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine build type and version
        id: determine
        run: |
          SHORT_HASH=$(git rev-parse --short HEAD)
          echo "short-hash=$SHORT_HASH" >> $GITHUB_OUTPUT
          
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            TAG="${{ github.ref }}"
            VERSION="${TAG#refs/tags/}"
            VERSION="${VERSION#v}"
            echo "build-type=release" >> $GITHUB_OUTPUT
            echo "version=$VERSION+${SHORT_HASH}" >> $GITHUB_OUTPUT
          else
            echo "build-type=nightly" >> $GITHUB_OUTPUT
            # Read version from package.json and append commit hash
            BASE_VERSION=$(jq -r '.version' package.json)
            echo "version=${BASE_VERSION}+nightly-${SHORT_HASH}" >> $GITHUB_OUTPUT
          fi

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    needs: determine-build-type
    strategy:
      fail-fast: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libssl-dev \
            libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            libayatana-appindicator3-dev \
            patchelf

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Update versions
        run: |
          jq '.version = "${{ needs.determine-build-type.outputs.version }}"' package.json > package.json.tmp
          mv package.json.tmp package.json
          jq '.version = "${{ needs.determine-build-type.outputs.version }}" | .bundle.macOS.bundleVersion = "${{ needs.determine-build-type.outputs.short-hash }}"' src-tauri/tauri.conf.json > src-tauri/tauri.conf.json.tmp
          mv src-tauri/tauri.conf.json.tmp src-tauri/tauri.conf.json

      - name: Install frontend dependencies
        run: bun ci

      - name: Build frontend
        run: |
          cd src-tauri; 
          cargo install sqlx-cli --no-default-features --features sqlite
          cargo sqlx database create; cargo sqlx migrate run;
          bun tauri:build -- --target x86_64-unknown-linux-gnu

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-artifacts
          path: src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/
          retention-days: 5

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    needs: determine-build-type
    strategy:
      fail-fast: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Update versions
        run: |
          jq '.version = "${{ needs.determine-build-type.outputs.version }}"' package.json > package.json.tmp
          mv -Force package.json.tmp package.json
          jq '.version = "${{ needs.determine-build-type.outputs.version }}" | .bundle.macOS.bundleVersion = "${{ needs.determine-build-type.outputs.short-hash }}"' src-tauri/tauri.conf.json > src-tauri/tauri.conf.json.tmp
          mv -Force src-tauri/tauri.conf.json.tmp src-tauri/tauri.conf.json

      - name: Install frontend dependencies
        run: bun ci

      - name: Build frontend
        run: |
          cd src-tauri; 
          cargo install sqlx-cli --no-default-features --features sqlite
          cargo sqlx database create; cargo sqlx migrate run;
          bun tauri:build -- --target x86_64-pc-windows-msvc

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: src-tauri/target/x86_64-pc-windows-msvc/release/bundle/
          retention-days: 5

  build-macos:
    name: Build macOS (${{ matrix.target }})
    runs-on: macos-latest
    needs: determine-build-type
    strategy:
      fail-fast: false
      matrix:
        target:
          - aarch64-apple-darwin
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Import Apple Developer Certificate
        run: |
          echo $APPLE_CERTIFICATE | base64 --decode > certificate.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          security import certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain
          security find-identity -v -p codesigning build.keychain

      - name: Update versions
        run: |
          jq '.version = "${{ needs.determine-build-type.outputs.version }}"' package.json > package.json.tmp
          mv package.json.tmp package.json
          jq '.version = "${{ needs.determine-build-type.outputs.version }}" | .bundle.macOS.bundleVersion = "${{ needs.determine-build-type.outputs.short-hash }}"' src-tauri/tauri.conf.json > src-tauri/tauri.conf.json.tmp
          mv src-tauri/tauri.conf.json.tmp src-tauri/tauri.conf.json

      - name: Install frontend dependencies
        run: bun ci

      - name: Build frontend
        run: |
          cd src-tauri; 
          cargo install sqlx-cli --no-default-features --features sqlite
          cargo sqlx database create; cargo sqlx migrate run;
          bun tauri:build -- --target ${{ matrix.target }}

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-${{ matrix.target }}-artifacts
          path: src-tauri/target/${{ matrix.target }}/release/bundle/
          retention-days: 5

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [determine-build-type, build-linux, build-windows, build-macos]
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release
          
          # Copy Linux AppImage
          if [ -d "artifacts/linux-artifacts/appimage" ]; then
            cp artifacts/linux-artifacts/appimage/*.AppImage* release/ 2>/dev/null || true
            cp artifacts/linux-artifacts/appimage/*.AppImage.tar.gz* release/ 2>/dev/null || true
          fi
          
          # Copy Windows MSI
          if [ -d "artifacts/windows-artifacts/msi" ]; then
            cp artifacts/windows-artifacts/msi/*.msi* release/ 2>/dev/null || true
          fi
          
          # Copy macOS DMG
          if [ -d "artifacts/macos-aarch64-apple-darwin-artifacts/dmg" ]; then
            cp artifacts/macos-aarch64-apple-darwin-artifacts/dmg/*.dmg* release/ 2>/dev/null || true
          fi
          
          echo "Release assets prepared:"
          ls -lah release/

      - name: Create Release with Assets
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.determine-build-type.outputs.version }}
          name: Release ${{ needs.determine-build-type.outputs.version }}
          body: |
            ## Release v${{ needs.determine-build-type.outputs.version }}
            
            **Build Information:**
            - **Version**: ${{ needs.determine-build-type.outputs.version }}
            - **Commit**: ${{ needs.determine-build-type.outputs.short-hash }}
            - **Release Date**: ${{ github.event.head_commit.timestamp }}
            
            ## Download
            Download the appropriate installer for your platform below.
            
            ### Supported Platforms
            - **Linux**: AppImage format (run with `chmod +x` and double-click)
            - **Windows**: MSI installer
            - **macOS**: DMG installer (arm64)
            
            ## Installation Instructions
            
            ### Linux
            ```bash
            chmod +x RAVN-*.AppImage
            ./RAVN-*.AppImage
            ```
            
            ### Windows
            Run the `.msi` file and follow the installer wizard.
            
            ### macOS
            Double-click the `.dmg` file and drag the app to Applications.
            
            ---
            
            ## Changes
            Add your changelog here
          files: release/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  nightly-notification:
    name: Nightly Build Notification
    runs-on: ubuntu-latest
    needs: [determine-build-type, build-linux, build-windows, build-macos]
    if: needs.determine-build-type.outputs.build-type == 'nightly' && always()
    steps:
      - name: Check build status
        run: |
          if [[ "${{ needs.build-linux.result }}" == "failure" || \
                "${{ needs.build-windows.result }}" == "failure" || \
                "${{ needs.build-macos.result }}" == "failure" ]]; then
            echo "::error::Nightly build failed on one or more platforms"
            exit 1
          else
            echo "✓ Nightly build completed successfully"
            echo "Version: ${{ needs.determine-build-type.outputs.version }}"
            echo "Commit: ${{ needs.determine-build-type.outputs.short-hash }}"
          fi
